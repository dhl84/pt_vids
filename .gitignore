# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# OS junk
.DS_Store

# Claude Code local/transient state (keep the skill, drop machine-local bits)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local model weights (large; download separately — see README)
models/

# Per-session artifacts: personal footage, mic recordings, transcripts and the
# generated cut lists/FCPXML all live next to the session, never in the repo.
*.srt
*.fcpxml
*.wav
*.mp4
*.mov
*.MP4
*.MOV
*.WAV
cuts.txt
review.md
_edit/
